#include <map>

int get_sum (std::map<int, int> & d_map);
void show_all (std::map<int, int> & d_map); 
int show_min (std::map<int, int> & d_map);
int show_max (std::map<int, int> & d_map);
std::map<int, int> handler(std::string * buf);
